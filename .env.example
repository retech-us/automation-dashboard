# Copy to .env (gitignored), then:
#   docker compose up --build
# Open http://127.0.0.1:8765/  — GenAI uses this key (same as web automation / GitHub secret OPENAI_KEY)
OPENAI_KEY=REPLACE_ME
OPENAI_MODEL=gpt-4.1
OPENAI_API_BASE=https://ai-api.symphonyretailai.com
